version: v2
# El go_package va declarado en el .proto, por eso managed mode queda apagado.
plugins:
  - local: protoc-gen-go
    out: gen
    opt: paths=source_relative
  - local: protoc-gen-go-grpc
    out: gen
    opt: paths=source_relative
